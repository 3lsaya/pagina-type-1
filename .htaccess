# Type Group — Redirects 301 para URLs limpias
# Requiere mod_rewrite habilitado en Apache

Options -Indexes
RewriteEngine On

# Forzar HTTPS
RewriteCond %{HTTPS} off
RewriteRule ^(.*)$ https://%{HTTP_HOST}/$1 [R=301,L]

# Redirigir www a sin www
RewriteCond %{HTTP_HOST} ^www\.(.+)$ [NC]
RewriteRule ^ https://%1%{REQUEST_URI} [R=301,L]

# URLs limpias → archivos .html
Redirect 301 /nosotros         https://typegroup.com.co/nosotros.html
Redirect 301 /contacto         https://typegroup.com.co/contacto.html
Redirect 301 /distribuidores   https://typegroup.com.co/distribuidores.html
Redirect 301 /politica-de-privacidad https://typegroup.com.co/politica-de-privacidad.html

# Tipos MIME correctos
AddType text/plain .txt
